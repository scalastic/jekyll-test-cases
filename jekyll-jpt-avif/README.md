# Reference Jekyll Site

- This Jekyll site is intended to be used as a `reference` in the Jekyll Test Cases:
    - It uses images
    - No additional Jekyll plugin is configured in `_config.yaml` nor `Gemfile`
